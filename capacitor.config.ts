import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.garmentapp.garmentapp',
  appName: 'garmentapp',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
